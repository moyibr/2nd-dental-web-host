# Public URL of the deployed site. Used for canonical URLs, Open Graph image
# resolution, robots.txt and sitemap.xml. Set this in Vercel once your domain
# is connected; without it the code falls back to https://moyibr.vercel.app.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
